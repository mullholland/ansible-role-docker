# {{ ansible_managed }}
# Collected Docker ENVIRONMENT Settings

# Use DOCKER_OPTS to modify the daemon startup options.
DOCKER_OPTS="{{ docker_opts_common | join(' ')}} {{ docker_opts_individual | join(' ')}}"
